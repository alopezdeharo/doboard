<?xml version="1.0" encoding="utf-8"?>
<!--
    task_item_root cubre toda la fila → toggle al tap en cualquier punto.
    android:clickable + android:focusable son necesarios en algunos launchers
    para que RemoteViews propague el fill-intent al root correctamente.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/task_item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical"
    android:minHeight="40dp"
    android:orientation="horizontal"
    android:paddingStart="2dp"
    android:paddingTop="4dp"
    android:paddingEnd="2dp"
    android:paddingBottom="4dp">

    <!-- Checkbox circular -->
    <TextView
        android:id="@+id/task_check"
        android:layout_width="22dp"
        android:layout_height="22dp"
        android:background="@drawable/widget_check_undone"
        android:gravity="center"
        android:textColor="@android:color/white"
        android:textSize="11sp"
        android:textStyle="bold" />

    <!-- Emoji frog (solo visible si isFrog = true) -->
    <TextView
        android:id="@+id/task_frog"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:textSize="11sp" />

    <!-- Título de la tarea -->
    <TextView
        android:id="@+id/task_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_weight="1"
        android:maxLines="2"
        android:textColor="@color/widget_on_surface"
        android:textSize="13sp" />

</LinearLayout>
